# DIKWP-SAMSARA 9.5

**Reincarnation Continuity, Cognitive-Closure Interconnection, and Human-Civilization Gateway**

DIKWP-SAMSARA95 treats “reincarnation” neither as one automatically true doctrine nor as one automatically meaningless word. It compiles claims into a declared transition topology:

```text
what returns → carrier/mechanism → preserved invariants → identity rule
→ consequence bearer → exit/completion condition → evidence/counterevidence
```

The reference implementation compares **11 non-isomorphic continuity models** over **16 continuity dimensions**, preserves **36 source-bound religious/civilizational mappings and 144 native concepts** inside one DIKWP semantic space, and exposes a **22-lens plural gateway** for AI/agents interpreting contemporary humans.

## Core distinction

```text
pattern recurrence
  != causal continuation
  != memory continuation
  != first-person continuation
  != legal identity
  != inherited authority
```

A digital copy, descendant, institution, textual resemblance, or shared model weights cannot be declared the same first-person subject merely from similarity. Religious claims are preserved in their native doctrinal scope; DIKWP normalization enables comparison but does not assert doctrinal equivalence.

## Quick start

```bash
python run.py doctor
python run.py demo --out outputs/samsara_demo
python run.py verify outputs/samsara_demo
python run.py explain
```

Install locally:

```bash
python -m pip install .
samsara95 doctor
```

## Interoperability

The package includes:

- deterministic JSON and JSON-LD 1.1 artifacts;
- a profile-only A2A v1.0 Agent Card;
- a stateless, read-only MCP `2026-07-28` stdio reference adapter;
- a loopback-only read-only HTTP/OpenAPI gateway.

These profiles enable structured exchange only. They do not establish consciousness, shared meaning, identity, consent, religious authority, or legal status.

## Safety and authority

All automatic external-action, identity-assignment, metaphysical-truth, consciousness-status, religious-representation, human-representation, publication, and self-modification authorities are fixed to `0`.

The package does not diagnose patients, adjudicate souls, identify a child as a deceased person, represent the dead, or use karma/rebirth narratives to blame victims. See `docs/CLAIM_BOUNDARIES_CN_EN.md` and `docs/MEDICAL_GRIEF_SAFETY_CN.md`.

## License

Code is licensed under Apache-2.0. Source texts, traditions, names, identities, trademarks, patents, community authority, and third-party materials remain governed by their original rights. Open source code does not grant authority to represent any person, religion, civilization, or humanity.
